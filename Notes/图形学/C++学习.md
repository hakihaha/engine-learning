---

mindmap-plugin: basic

---

# C++零基础到工程实践笔记

## 第一章
- 创建项目
    - 一个解决方案可以有多个项目
    - 一个解决方案可以有一个项目
    - hello world程序
-

## include
-
    - <>
        - 表示从系统路径查找
    - iostream
        - 相当于库文件
- 程序生成过程中每种文件的作用
    - .cpp
        - 源文件
    - sln
        - 解决方案文件
    - vcxproj
        - 项目配置文件
    - .filters
        - 文件分类配置
    - .user
        - 用户特定设置
    - .obj
        - 是源文件编译后生成的二进制文件
    - .exe
        - 将所有.obj文件合并生成最终可执行文件
- 程序执行的过程
    - 预处理
    - 编译
    - 汇编
    - 链接
    - 执行

## 第二章，变量和内存的关系
- 基础类型
- 初识变量
- 变量的算术运算和浮点数与整数转换
- 变量的作用域和生命周期
- 什么是常量
- auto的使用

## 第三章逻辑
- 逻辑判断IF
    - 算术运算符
        - 逐位非（~）
            - 0——>1
            - 1——>0
        - 逐位与（&）
            - 只有两个为ture才为真
            - 双开为开，有闭则闭
        - 逐位或（|）
            - 只要有一个为真就是真
    - 逻辑运算符
        - 非（！）
            - 代用运算符not
        - 与（&&）
            - 代用运算符and
        - 或（||）
            - 代用运算符or
    - 比较运算符
- string
    - 定义语法
        - 头文件
            - string
    - 字符串长度和截断
        - size()
            - 只是长度
        - 中文处理
            - GBK编码下：一个汉字等于2个字节=长度计数1
    - 字符串和整数浮点数转换
        - 字符串转数字
            - 转换函数
                - 字符串转整型stoi()
                - 字符串转双精度stod()
                - 字符串转化为float类型  stof（）
                - 字符串转换为long long类型  stoll ()
        - 数字转换成字符
            - 转换成函数
                - to_string()
    - 字符串判断和拼接
    - 查找和替换指定字符串
        - string.find()
        - string.replace(查找的起始位置，被替换内容的长度，新字符内容)
- 枚举
    - C++之前的枚举
    - C++之后的枚举
- main函数参数传递
- 案例
    - 日志模块
        - debug
        - info
        - error
        - fatal
- 作业